// No external scanner needed: comments are handled by a high-precedence
// internal token in grammar.js (token(prec(2, /\*[^\n]*/))).
// This file is kept as a placeholder in case a scanner is needed later.
